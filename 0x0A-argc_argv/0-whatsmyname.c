#include <stdio.h>
#include "main.h"
/**
 * main - prints its name with \n
 * @argc: number of arguments entered
 * @argv: array pointer for the arguements
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
